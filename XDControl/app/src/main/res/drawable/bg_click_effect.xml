<?xml version="1.0" encoding="utf-8"?><!-- res/drawable/bg_click_effect.xml -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#20000000"> <!-- 涟漪颜色（半透明黑色） -->
    <!-- 默认背景色 -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#000" /> <!-- 控件默认背景色 -->
            <corners android:radius="180dp" /> <!-- 圆角（可选） -->
        </shape>
    </item>
</ripple>